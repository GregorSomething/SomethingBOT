const Discord = require('discord.js')
const {RichEmbed } = require('discord.js');
module.exports = {
    name: 'prefix',
    execute(message, args, PREFIX){
        // only here for info in help
        
        
    }
}